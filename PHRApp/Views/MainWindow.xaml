<Window x:Class="PHRApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PHRApp.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal"
                    Margin="10"
                    Grid.Row="0">
            
            <TextBox Width="200"
                     Margin="0,0,10,0"
                     Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" />

            <ComboBox Width="200"
                      Margin="0,0,10,0"
                      ItemsSource="{Binding Categories}"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      SelectedValue="{Binding SelectedCategoryId}" />

            <Button Content="Szukaj"
                    Width="100"
                    Click="OnSearchClicked" />

            <Button Content="Dodaj wpis"
                    Width="100"
                    Margin="10,0,0,0"
                    Click="OnAddEntryClicked" />
        </StackPanel>
        
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Entries}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="10"
                  MouseDoubleClick="OnEntryDoubleClicked"
                  SelectedItem="{Binding SelectedEntry}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" />
                <DataGridTextColumn Header="Event Date"
                                    Binding="{Binding EventDate, StringFormat={}{0:yyyy-MM-dd HH:mm}}" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" />
                <DataGridTextColumn Header="Category"
                                    Binding="{Binding CategoryNamesDisplay}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
